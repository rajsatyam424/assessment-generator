version: "3.9"

services:
  api:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - ASSESSMENT_OUTPUT_DIR=/data/assessments
    volumes:
      - assessment_data:/data/assessments
    restart: unless-stopped

volumes:
  assessment_data:
